name: CI/CD

on:
  push:
    branches: [master]

jobs:
  build-deploy:
    runs-on: docker
    container:
      image: docker:24.0.5
      options: --privileged
    steps:
      - name: Check Docker
        run: |
          export DOCKER_HOST=tcp://127.0.0.1:2376
          export DOCKER_TLS_VERIFY=1
          export DOCKER_CERT_PATH=/certs/client
          docker info

      - name: Login to registry
        env:
          REGISTRY_USER: ${{ secrets.REGISTRY_USER }}
          REGISTRY_PASSWORD: ${{ secrets.REGISTRY_PASSWORD }}
        run: |
          echo "$REGISTRY_PASSWORD" | docker login -u "$REGISTRY_USER" --password-stdin forgejo.ioalexander.com

      - name: Build & push backend
        run: |
          docker build -t forgejo.ioalexander.com/ioalexander/backend:latest ./backend
          docker push forgejo.ioalexander.com/ioalexander/backend:latest

      - name: Build & push frontend
        run: |
          docker build -t forgejo.ioalexander.com/ioalexander/frontend:latest ./frontend
          docker push forgejo.ioalexander.com/ioalexander/frontend:latest

      - name: Setup kubectl
        run: |
          curl -LO https://dl.k8s.io/release/v1.27.0/bin/linux/amd64/kubectl
          chmod +x kubectl && mv kubectl /usr/local/bin/

      - name: Deploy to k8s
        run: |
          echo "${{ secrets.KUBECONFIG }}" > kubeconfig
          export KUBECONFIG=$PWD/kubeconfig
          kubectl apply -f k8s/
